import { Settings } from "lucide-react"
import { ComingSoonPage } from "@/components/ComingSoonPage"

export default function SettingsPage() {
  return (
    <ComingSoonPage
      title="Settings"
      description="Application preferences, notifications, and account settings — coming soon."
      icon={Settings}
    />
  )
}
